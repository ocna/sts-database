# behat.yml
default:
  paths:
    features: '%behat.paths.base%/../features'
    bootstrap:  '%behat.paths.features%/bootstrap'
  extensions:
    Behat\MinkExtension\Extension:
      base_url: http://dev.stsdatabase.org
      goutte: ~
      sahi: ~
  context:
    parameters:
      browser: firefox
